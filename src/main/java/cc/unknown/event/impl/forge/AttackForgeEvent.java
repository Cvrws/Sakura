package cc.unknown.event.impl.forge;

import net.minecraftforge.event.entity.player.AttackEntityEvent;

public class AttackForgeEvent extends ForgeEvent<AttackEntityEvent> {
    public AttackForgeEvent(AttackEntityEvent event) {
        super(event);
    }
}